$(document).ready(function() {


    // Reminder: we have /e/:post_id
    // and we also have  /e/:post_id/:comment_id

    // Function that submits edit
    $('#text-submit').on('click', function() {

    })

})